{
  "name": "ts-space-demo",
  "version": "0.0.1-alpha.0",
  "description": "Implementation of a space DSL with LionWeb",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LionWeb-io/space-demo.git"
  },
  "bugs": {
    "url": "https://github.com/LionWeb-io/space-demo/issues"
  },
  "devDependencies": {
    "rimraf": "6.0.1"
  },
  "scripts": {
    "build": "npm run build -w packages --if-present && npm run check -w packages --if-present",
    "clean": "npm run clean -w packages --if-present && npx rimraf .parcel-cache node_modules package-lock.json"
  },
  "private": true,
  "workspaces": [
    "./packages/build",
    "./packages/space-dsl",
    "./packages/daga-client",
    "./packages/echo-ws-server",
    "./packages/react-client",
    "./packages/test"
  ]
}
